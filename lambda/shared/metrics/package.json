{
    "name": "metrics",
    "version": "1.0.0",
    "description": "CloudWatch metrics emission utility for Lambda functions",
    "main": "dist/index.mjs",
    "scripts": {
        "build": "node build.mjs",
        "build:tsc": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest --run"
    },
    "dependencies": {
        "@aws-sdk/client-cloudwatch": "^3.600.0"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "typescript": "^5.4.5",
        "vitest": "^1.6.0"
    },
    "type": "module"
}