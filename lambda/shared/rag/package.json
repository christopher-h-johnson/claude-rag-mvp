{
    "name": "rag",
    "version": "1.0.0",
    "description": "RAG orchestration module coordinating embedding generation and vector search",
    "main": "dist/index.mjs",
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "build:tsc": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest --run"
    },
    "dependencies": {
        "@aws-sdk/client-bedrock-runtime": "^3.621.0",
        "@opensearch-project/opensearch": "^2.12.0",
        "@aws-sdk/credential-provider-node": "^3.0.0",
        "ioredis": "^5.4.1"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "typescript": "^5.4.5",
        "vitest": "^1.6.0"
    }
}