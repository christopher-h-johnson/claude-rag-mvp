{
    "name": "embeddings",
    "version": "1.0.0",
    "description": "AWS Bedrock Titan Embeddings generator",
    "main": "dist/index.mjs",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "test": "vitest --run"
    },
    "dependencies": {
        "@aws-sdk/client-bedrock-runtime": "^3.621.0"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "typescript": "^5.4.5",
        "vitest": "^1.6.0"
    },
    "type": "module"
}