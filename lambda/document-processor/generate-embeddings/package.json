{
    "name": "generate-embeddings-lambda",
    "version": "1.0.0",
    "description": "Lambda function to generate embeddings for document chunks",
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@aws-sdk/client-bedrock-runtime": "^3.0.0",
        "@aws-sdk/client-lambda": "^3.0.0",
        "@aws-sdk/client-s3": "^3.0.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    }
}