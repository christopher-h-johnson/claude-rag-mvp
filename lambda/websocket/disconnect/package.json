{
    "name": "websocket-disconnect",
    "version": "1.0.0",
    "description": "WebSocket disconnection handler Lambda",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc && cd dist && zip -r ../index.zip .",
        "clean": "rm -rf dist index.zip"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.600.0",
        "@aws-sdk/lib-dynamodb": "^3.600.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.138",
        "@types/node": "^20.12.12",
        "typescript": "^5.4.5"
    }
}